import { Connector, WalletChain } from '@soroban-react/types';
import React from 'react';
import * as SorobanClient from 'soroban-client';
/**
 * @param children - A React subtree that needs access to the context.
 */
export interface SorobanReactProviderProps {
    appName?: string;
    autoconnect?: boolean;
    chains: WalletChain[];
    activeChain?: WalletChain;
    children: React.ReactNode;
    connectors: Connector[];
    server?: SorobanClient.Server;
}
export declare function SorobanReactProvider({ appName, autoconnect, chains, activeChain, // Non mandatory fields default to default Context fields value
children, connectors, server, }: SorobanReactProviderProps): React.JSX.Element;
