import { Connector, WalletChain } from '@soroban-react/types';
import React from 'react';
import * as SorobanClient from 'soroban-client';
export declare const defaultSorobanContext: SorobanContextType;
export interface SorobanContextType {
    autoconnect?: boolean;
    appName?: string;
    chains: WalletChain[];
    connectors: Connector[];
    activeChain?: WalletChain;
    address?: string;
    activeConnector?: Connector;
    server?: SorobanClient.Server;
    connect: () => Promise<void>;
    disconnect: () => Promise<void>;
    setActiveChain?: (chain: WalletChain) => void;
}
export declare const SorobanContext: React.Context<SorobanContextType | undefined>;
